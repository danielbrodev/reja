<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reja</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <div class="container">
      <h1 class="display-4 text-center py-1">계획과 목표</h1>
      <div class="jumbotron shadow-sm">
        <form action="/create-item" method="post" id="create-form">
          <div class="d-flex align-items-center">
            <input
              id="create-field"
              type="text"
              name="reja"
              class="form-control m-3 mb-0"
              style="flex: 1"
              autocomplete="off"
              placeholder="계획을 입력하세요"
              autofocus
            />
            <button class="btn btn-primary m-3 mb-0">새 계획 추가</button>
          </div>
        </form>
        <hr />
        <ul id="item-list" class="list-group p-3 pt-0">
          <% items.map(function(item){ %>
          <li
            class="list-group-item d-flex justify-content-between align-items-center"
          >
            <span><%= item.reja%></span>
            <div class="btn-group btn-group-sm">
              <button data-id="<%=item._id%>" class="edit-me btn btn-outline-primary">수정</button>
              <button data-id="<%=item._id%>" class="delete-me btn btn-outline-danger">삭제</button>
            </div>
          </li>

          <% }) %>
        </ul>
      </div>
      <button id="clean-all" class="btn btn-danger mt-3">모든 계획 삭제</button>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script src="./browser.js"></script>
  </body>
</html>
